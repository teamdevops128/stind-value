- script: |
    echo "Agent OS: $(Agent.OS)"
    if [ "$(Agent.OS)" == "Windows_NT" ]; then
      echo "Running on Windows agent"
    else
      echo "Running on Linux agent"
    fi
  displayName: 'Detect Agent OS'
